import 'package:flutter/material.dart';

class EdgeCon {
  static const EdgeInsets card = EdgeInsets.all(8.0);
  static const EdgeInsets chart = EdgeInsets.all(24.0);
  static const EdgeInsets miniBaslik = EdgeInsets.only(bottom: 8.0);
  static const edgeSizedTop = SizedBox(
    height: 40,
  );
}
